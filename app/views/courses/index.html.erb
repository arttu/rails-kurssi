<h2>Courses</h2>

<h5>> <%= link_to("Create a new course", new_course_path) %></h5>

<div>
<% @courses.each do |course| %>
	<div class="course index">
		<div><%= link_to course.name, course_path(course), :class => 'name' %></div>
	</div>
<% end -%>
</div>